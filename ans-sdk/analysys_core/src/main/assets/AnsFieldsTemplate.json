{
  "base": {
    "outer": [
      "appid",
      "xwho",
      "xwhat",
      "xwhen",
      "xcontext"
    ],
    "xcontext": [
      "$lib",
      "$lib_version",
      "$platform",
      "$debug",
      "$is_login",
      "$device_id"
    ]
  },
  "$startup": {
    "xcontext": [
      "$channel",
      "$time_zone",
      "$manufacturer",
      "$app_version",
      "$model",
      "$os",
      "$os_version",
      "$network",
      "$carrier_name",
      "$screen_width",
      "$screen_height",
      "$brand",
      "$language",
      "$is_first_day",
      "$session_id",
      "$is_first_time",
      "$is_from_background",
      "$is_time_calibrated",
      "$start_source"
    ]
  },
  "$end": {
    "xcontext": [
      "$channel",
      "$time_zone",
      "$manufacturer",
      "$app_version",
      "$model",
      "$os",
      "$os_version",
      "$network",
      "$carrier_name",
      "$screen_width",
      "$screen_height",
      "$brand",
      "$language",
      "$is_first_day",
      "$session_id",
      "$is_time_calibrated"
    ]
  },
  "$track": {
    "xcontext": [
      "$channel",
      "$time_zone",
      "$manufacturer",
      "$app_version",
      "$model",
      "$os",
      "$os_version",
      "$network",
      "$carrier_name",
      "$screen_width",
      "$screen_height",
      "$brand",
      "$language",
      "$is_first_day",
      "$session_id",
      "$pagename",
      "$is_time_calibrated"
    ]
  },
  "$pageview": {
    "xcontext": [
      "$channel",
      "$time_zone",
      "$manufacturer",
      "$app_version",
      "$model",
      "$os",
      "$os_version",
      "$network",
      "$carrier_name",
      "$screen_width",
      "$screen_height",
      "$brand",
      "$language",
      "$is_first_day",
      "$session_id",
      "$is_time_calibrated"
    ]
  },
  "$app_click": {
    "xcontext": [
      "$channel",
      "$time_zone",
      "$manufacturer",
      "$app_version",
      "$model",
      "$os",
      "$os_version",
      "$network",
      "$screen_width",
      "$screen_height",
      "$carrier_name",
      "$brand",
      "$language",
      "$is_first_day",
      "$session_id"
    ]
  },
  "$user_click": {
    "xcontext": [
      "$channel",
      "$time_zone",
      "$manufacturer",
      "$app_version",
      "$model",
      "$os",
      "$os_version",
      "$network",
      "$screen_width",
      "$screen_height",
      "$carrier_name",
      "$brand",
      "$language",
      "$is_first_day",
      "$session_id"
    ]
  },
  "$first_installation": {
    "xcontext": [
      "$channel",
      "$time_zone",
      "$manufacturer",
      "$app_version",
      "$model",
      "$os",
      "$os_version",
      "$network",
      "$carrier_name",
      "$screen_width",
      "$screen_height",
      "$brand",
      "$language",
      "$is_first_day",
      "$session_id",
      "$pagename",
      "$is_time_calibrated"
    ]
  },
  "$alias": {
    "xcontext": [
      "$imei",
      "$mac"
    ]
  },
  "$profile": {
    "xcontext": [
      "$imei",
      "$mac"
    ]
  }
}